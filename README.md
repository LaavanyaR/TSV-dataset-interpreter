# TSV-dataset-interpreter
Loads a TSV file (from a local path, GitHub URL, or manual upload) → interprets its schema (column types, null counts, uniques) → converts the full dataset to CSV/JSON/XML → lets you pick key fields and publishes them as a downloadable tabular report. One page, one form, one click to download — no separate backend needed.
